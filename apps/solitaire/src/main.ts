import './app/app';

/* npx create-nx-workspace solitaire --preset=apps

npm run nx add @nx/web

npm run nx g @nx/web:app -- --name=solitaire --directory=apps/solitaire \
--bundler=vite --unitTestRunner=vitest \
--tags=scope:browser --style=scss --strict=true --linter=eslint --e2eTestRunner=playwright

npm i -D @babylonjs/inspector gh-pages sass-embedded type-fest

npm i -S \
@babylonjs/core \
@babylonjs/gui \
@babylonjs/loaders \
@babylonjs/havok \
inversify miniplex \
@babylonjs/materials

update .gitignore
env.*
*.env */
